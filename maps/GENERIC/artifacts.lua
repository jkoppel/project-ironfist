function OnArtifactGive(hero, id)

end;

function OnArtifactTake(hero, id)

end;

function OnCalcMobility(hero, mobility)

end;

function OnCalcManaCost(hero, spell, cost)

end;

function OnCalcSpellChance(army, spell, chance)

end;

function OnCalcLuck(hero, army, town, luck)

end;

-- function OnGetLuck(hero) -- new
	-- if HasArtifact(h, ARTIFACT_MASTHEAD) then
		-- return 1;
	-- end;
	-- return -1;
-- end;

-- function OnGetMorale(hero) -- new
	-- if HasArtifact(h, ARTIFACT_MASTHEAD) then
		-- return 1;
	-- end;
	-- return -1;
-- end;

-- function OnGetLuckInfo(hero) -- new
	-- if HasArtifact(h, ARTIFACT_MASTHEAD) then
		-- return "Masthead bonus at sea +1";
	-- end;
	-- return '';
-- end;

-- function OnGetMoraleInfo(hero) -- new
	-- if HasArtifact(h, ARTIFACT_MASTHEAD) then
		-- return "Masthead bonus at sea +1";
	-- end;
	-- return '';
-- end;